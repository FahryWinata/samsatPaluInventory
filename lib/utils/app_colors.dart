import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - Blue/Teal scheme from the design
  static const Color primary = Color(0xFF00ACC1); // Cyan/Teal
  static const Color primaryDark = Color(0xFF00838F);
  static const Color primaryLight = Color(0xFF4DD0E1);

  // Accent Colors
  static const Color accent = Color(0xFF0097A7);
  static const Color accentLight = Color(0xFF26C6DA);

  // Background Colors
  static const Color background = Color(0xFFF5F5F5); // Light gray
  static const Color cardBackground = Colors.white;
  static const Color sidebarBackground = Color(0xFF263238); // Dark blue-gray

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Colors.white;

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);
  static const Color error = Color(0xFFEF5350);
  static const Color info = Color(0xFF42A5F5);

  // Asset Status Colors
  static const Color available = Color(0xFF66BB6A);
  static const Color assigned = Color(0xFF42A5F5);
  static const Color maintenance = Color(0xFFFF7043);

  // Neutral Colors
  static const Color divider = Color(0xFFE0E0E0);
  static const Color shadow = Color(0x1A000000);
}
